import 'package:flutter/material.dart';

class SupportTicketAppbarLayout extends StatelessWidget {
  const SupportTicketAppbarLayout({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}